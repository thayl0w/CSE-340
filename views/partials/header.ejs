<header id="top-header">
    <span class="siteName">
        <a href="/" title="Return to home page">CSE Motors</a>
    </span>
    <div id="tools">
        <% if (locals.user) { %>
            <span>Welcome <%= user.account_firstname %></span>
            <a href="/account/logout" style="margin-left: 1rem;">Logout</a>
        <% } else { %>
            <a href="/account/login">My Account</a>
        <% } %>
    </div>
</header>